name: velvet_basic_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.0.0
  velvet_annotation: ^0.0.1-dev.4
  velvet_framework: ^0.0.1-dev.7

dev_dependencies:
  build_runner: ^2.4.9
  flutter_test:
    sdk: flutter
  velvet_generator: ^0.0.1-dev.5
  velvet_lints: ^0.0.1-dev.2

flutter:
  uses-material-design: true
