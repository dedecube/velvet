name: velvet_basic_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  velvet_framework: ^0.0.1-dev.3
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  velvet_lints: ^0.0.1-dev.2

flutter:
  uses-material-design: true
